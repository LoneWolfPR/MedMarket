// Package ent contains the generated Ent client. Entity schemas live in
// ./schema; run `task backend:generate` (or `go generate ./ent`) to regenerate.
package ent

//go:generate go tool ent generate ./schema

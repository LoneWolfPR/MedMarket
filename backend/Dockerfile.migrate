# Migration runner: the Atlas CLI plus the versioned migrations directory, run as
# an initContainer before the backend/worker start. Pinned to the latest
# published image tag on the same 1.2.x line as local (`atlas version`) so the
# atlas.sum integrity check matches. Build context is ./backend.
FROM arigaio/atlas:1.2.3

COPY ent/migrate/migrations /migrations
